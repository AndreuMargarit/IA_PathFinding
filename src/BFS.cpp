#include "BFS.h"

void BFS::GeneratePath(Graf graph, Vector2D startPosition, Vector2D targetPosition)
{
	std::map<int, Node*> cameFrom;
	std::queue<Node*> queue;

	if (!cameFrom.count(23) > 0) {
		//No hem explorat el node
	}
	//generatedPath.push_back(Node(*graph.GetNode(startPosition)));

}
